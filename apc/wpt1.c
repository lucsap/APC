#include <stdio.h>
#include <stdlib.h>
#include <math.h>

struct complexo {
	
};

convComplexo() {



	if() {


		return E;
	}else {
		return C;
	}
}

complexOp() {
	if() {
		return (struct complexo);
	}else {
		return (struct complexo);
	}
}

int main () {

    int z, re, x, y, e, j;
    scanf("%lld %lld %lld", &a, &b, &c);
    delta = ((b*b) + ((-4)*(a))*(c));

    if (delta >= 0) {
        printf("reais\n");
    }else {
        printf("complexas\n");
    }
    return 0;
}